# EC backend の API ベース URL（末尾の /api/ec まで含める）。
# 開発: EC backend は WMS(8000) と被らないよう 8001 で起動する規約。
#   cd ../backend && uv run python manage.py runserver 8001
# 本番: https://ec.komaki-wms.com/api/ec など。
VITE_API_BASE_URL=http://localhost:8001/api/ec
